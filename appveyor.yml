version: 1.0.{build}
build_script:
  - ps: 'powershell-helpers/generate.ps1'

artifacts:
  - path: '**\*.nupkg'
    name: nupkg
  - path: '**\*.nuspec'
    name: nuspec
  - path: '**\.version'
    name: version

